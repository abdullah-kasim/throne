# ⚪ OPEN OBJECTIVES — nothing dispatched — last reconciled 2026-08-13 00:45

**Rescued from blocks archived on 2026-08-13. Every campaign in those blocks was
verified LANDED against `git log --grep="Deliver <agent>"` before its bullet was
marked; these three could NOT be verified and are therefore still live.**

- ⚪ **DUP — two harnesses, one worktree.** Observed live during the BPE campaign.
  No Deliver commit anywhere in throne history. Genuinely open.
- ⚪ **BRG2 — the Runner's burger menu.** `alpha-brg2-burger-menu`. **Different
  repo**, so throne's history cannot prove or disprove delivery — do not treat the
  missing commit as failure. Verify in the Runner's own repo before redispatching.
- ⚪ **HRN — harness update (Claude Code + Codex), TESTED.** Dispatched 2026-08-11.
  **No Deliver commit in throne history.** Note the Regent caused real damage here:
  it invented a false regression correlation and HRN reverted correct work on its
  say-so; the true cause was `throne-work` running code loaded hours earlier. Check
  what actually landed before assuming anything about this objective's state.

- ⚪ **Hole-2 (from EVD `fd76d55`)** — a mid-flight Regent evidence mandate is only
  caught once an Alpha transcribes it into ASSIGNMENT.md. Author-remembered: the
  same failure class EVD fixed, one level up.
- ⚪ **Blocked-vs-wedged (from STA `a78e7da`)** — the `{"blocked":true}` marker can
  only be emitted ON REGENT INSTRUCTION, so an Alpha awaiting a ruling has no path
  to self-mark: the only thing that would tell it to is the ruling it is waiting on.
  STA wants a live incident before more heuristics get written in the dark.
- ⚪ **SWR** — guards fire during the spawn window and report phantom problems.
- ⚪ **ATS** — spawning a Shadow and TASKING it are two steps, and the second is
  optional.
- ⚪ **BMS-PROMO** — cellstra-plc `bms-iot-module` → `main`. Another repo, and
  explicitly the Lord's decision.

## 🔴 THE LORD'S OWN OPEN ORDERS — rescued 2026-08-13 before bulk-archiving. OTHER REPOS; verify there, not in throne's history.
- ⚪ **KDC — datasheet cache keyed by LCSC part number.** Was gated on `alpha-ksv-severity-legend` landing.
- ⚪ **K3S — run EXAMPLE-BOARD through single-slice mode.** Dependency-gated. (K1S landed `be0ef68f1`: single-slice shipped as a MODE, multi-facet still default.)
- ⚪ **KIR-2 — EXAMPLE-BOARD PSU and V-I Sensors** design-tool issue report. SERIAL: do not start until KIR-1's successor state is confirmed. (KIR-1 delivered 2026-08-09.)
- ⚪ **KIR-3 — EXAMPLE-BOARD Cell Monitor newMCU** design-tool issue report. SERIAL, after KIR-2.
- 🟡 **RBP — the Runner beauty pass. HELD BY THE LORD** 2026-08-09: *"let's not do rbp yet"*. Do not dispatch without his word.
- ⚪ **BMS-PROMO — cellstra-plc `bms-iot-module` → `main`.** Explicitly the Lord's decision.

## 📌 STANDING LAW — carried forward so the archive can be cleared safely
- **Tool-behaviour findings go to `agent_docs/MEMORY/`** (survives reap); campaign story goes to `REPORT.md`.
- **No more `uninstall.sh` behavioural verification on this box, by anyone, ever.** It is edited, never executed.
- **The Lord's migration law:** keep the old path until the new one EARNS retirement — replacement ships alongside what it replaces, the fallback gets its own name, retirement criteria are written BEFORE starting. (Honoured tonight: the keep-going timer was retired only after the hosted cron was watched working.)
- **Landing slots** are Regent-granted, one campaign at a time; absorb before requesting.
- **Verify at source, never from the report.** Content-diff before authorising a merge.
- **A dead agent with a landed delivery but unfinished obligations is RESUMED, not reaped.**
- **Never invoke an interactive prompt** — an agent that does deadlocks itself permanently, unreachable even by send-agent. Recovery: `herdr agent send-keys <name> esc`.

- ⚪ **BOXLOCK — the one-suite-per-box rule cannot be held by discipline; make the runner enforce it.** TWO independent agents broke it on 2026-08-13 (`alpha-bmq-bullmq-cutover`, `alpha-amq2-mark-position`), both explicitly briefed, both self-reported, both correctly discarded their own contaminated results. The rule is EVIDENCED (two concurrency guards failed 3-of-4 under a concurrent suite, 10/10 alone) but unenforced. Wanted: a lock the test runner itself takes, so a second suite REFUSES rather than silently corrupting both. The Lord's principle: *"cant trust agent to solidly follow instructions, but you can trust code."* ⛔ **(FALSE AUTO-MARK on BOXLOCK — REVERTED BY THE REGENT 2026-08-13 05:10: this bullet is an OPEN objective that merely NAMES an agent; the marker matched the mention, not the objective. See AMQ-3.)**
- ⚪ **CORRECTED — `trim-queue-runtime` DID have test coverage; the gap was the CONTRACT BETWEEN producer and consumer.** The earlier entry here claimed ZERO coverage and the Regent relayed that to the Lord and built a causal argument on it. **BOTH WERE WRONG.** `src/trim-queue/trim-queue-runtime.test.ts` has existed on main since `6e39ad1` with **8 tests, 271 lines**. `alpha-amq2-mark-position` had accidentally CLOBBERED that file with a 57-line replacement using Write, then reported it as "first-ever unit tests" — it caught its own error reading 99b's line-count table (a suspicious 271→57 "shrink" on a file it believed it had created), restored the original verbatim and appended its 2 new tests on top (`84f91ac`, now 308 lines, 10 passing). ⛔ **(FALSE AUTO-MARK on CORRECTED — REVERTED BY THE REGENT 2026-08-13 05:10: this bullet is an OPEN objective that merely NAMES an agent; the marker matched the mention, not the objective. See AMQ-3.)**
  **The corrected and sharper finding:** producer and consumer were EACH tested in isolation; **nothing tested the SEAM between them.** So a mark that `trim-queue` did not recognise looked like success at every layer that was tested. **Coverage of two components is not coverage of their contract** — that is how AMQ shipped broken, and AMQ-2's mark→trim end-to-end proof is what actually closes it.
- ✅ **CAG — route `create-agent`'s opening prompt through the centralised send path.** **(auto-marked at reap: VERIFIED LANDED 469593d187c43de9468965dd8e2dfa1b95c87a8f)** THE LORD'S RULE: *"do not, I repeat, do not bypass send-agent's rest API whenever agent message sending is needed."* **TRIGGER: dispatch once `alpha-bmq-bullmq-cutover`'s fixes have LANDED** — the conversion needs the delivery payload schema extended in BOTH backends (`omitSenderAttribution`, `waitForStartupQuiescence`, `disableFileBackedDelivery` are load-bearing for a fresh pane's first turn), and the BullMQ backend was under repair tonight after a failed live flip. **This is a TIME-BOXED, STATED deviation from the Lord's rule, not an exception.** MSG established it is NOT a live corruption bug — `recipient-pane-lock.service.ts`'s per-pane kernel flock already serialises direct against queued writes — so it is an observability/centralisation gap, which is why the delay is tolerable. **The Regent told the Lord it was a live race; MSG's evidence corrected that.** ⛔ **(FALSE AUTO-MARK — REVERTED BY THE REGENT 2026-08-13 05:10: OPEN objective, matched only because its text NAMES an agent. See AMQ-3.)**

- ⚪ **STATUSLAG — agent status flips to `done` AFTER the agent is finished, so `--force` is becoming routine.** `alpha-msg-central-send` had to `--force` FOUR of its five gate Shadows because each had landed REPORT.md and reported DONE while herdr still showed `working`. The Regent hit the same lag reaping KGR and AMQ. **THE DANGER IS NOT THE LAG, IT IS THE HABIT:** `--force` cascades to live children and exists to prevent killing working agents. If the normal path routinely requires it, the guard stops being a guard and becomes a prompt people click through — and one day it kills a Shadow that really was working. Same family as the Regent's false stall call and KGR's pgrep false-positive: **the court's view of an agent's state lags reality, and every consumer compensates differently.** Wanted: reap should consult the durable completion signal (REPORT.md / delivery verdict) BEFORE the laggy pane status, so a finished agent reaps cleanly without `--force`.

- 🔴 **TESTISO — the test suite can reach the LIVE Redis and the PRODUCTION delivery queue. Found 2026-08-13 07:40, minutes after the BullMQ flip.** Measured in `throne-redis`: **4 failed jobs in `bull:throne-message-delivery:failed`**, all `no herdr agent named "recipient"` — a literal placeholder recipient, i.e. a TEST enqueueing into the REAL production queue. They failed harmlessly ONLY because no agent is named "recipient"; **a test using a real agent name would have delivered a spurious message into a LIVE PANE.** Also **5,642 test keys vs 25 production keys** — 99.6% of the live Redis is test litter, accumulating unbounded.
  **This hazard did not exist before 07:20.** Those enqueues went to SQLite and were inert; the flag being ON routed them into production Redis, and the flag lives in a config file every test process inherits by default (`XDG_CONFIG_HOME` + `DEFAULT_BULLMQ_REDIS_URL=127.0.0.1:6379`).
  **DO NOT FIX THIS WITH DISCIPLINE.** "Every campaign must pin a scratch config" is the same shape as the one-suite-per-box rule, **which two independent agents broke on the same night despite explicit briefing.** The Lord's principle applies: *"cant trust agent to solidly follow instructions, but you can trust code."* Wanted: tests structurally unable to reach the production Redis or the production queue name — a refusal, not a convention. Related: [[BOXLOCK]].
  **Separate sub-finding to report, not yet located:** the code path that enqueues to the PRODUCTION queue name using a placeholder recipient. `alpha-bmq3-worker-leak` is grepping for it and will report file+line without fixing it.
  **The 5,642 stale test keys are the REGENT'S to clear, not a campaign's** — deleting from a Redis that now carries live delivery is not a campaign action.

- 🔴 **AMQ-4 — AMQ-3's narrowing BROKE 3 of AMQ-2's tests and its gate passed anyway. Live red on main.** Diagnosed by the Regent 2026-08-13 11:10.
  `bulletReferencesAgent` now calls `leadingIdentifierSpan(line)` and returns FALSE when a bullet has **no bold span at all**. AMQ-2's real-delivery e2e fixtures are plain bullets — `- ${name} ships it` — so the marker no longer matches them. Failing on main now: *"a genuinely delivered agent gets its bullet marked"*, *"--force on a delivered agent STILL marks"*, *"a clean FAST-FORWARD delivery still gets marked under --force"*. **The three REFUSAL tests still pass** — so the narrowing failed in the SAFE direction, and the miss is loud (`could not confidently mark`). That is why this is not an emergency.
  **But three permanently-red tests on main is exactly how the 557-line cap breach hid for hours** — every campaign since triaged it as "known pre-existing" and moved on. A standing red is a place regressions go to be ignored.
  **The design question to settle, not just the fix:** must a campaign bullet be BOLD to be auto-markable? Real dispatched bullets are (`- 🟢 **AMQ — \`alpha-amq-autumark-queue\`**`), so the rule matches practice — but that is now an UNDOCUMENTED FORMATTING CONTRACT on the Regent's own writing, and AMQ-3 was already told to document the leading-span contract. Options: accept a plain bullet whose FIRST TOKEN is the agent name (positional, still not prose); or update AMQ-2's fixtures to the real bold convention IF that convention is genuinely mandatory. **Do not "fix" it by falling back to whole-line matching — that reintroduces the prose-collision bug that ate three of the Lord's open objectives.**
  **Process finding, worth more than the fix:** AMQ-3 ran a full 99d and shipped anyway. Either the suite was not run or these were triaged as pre-existing. Its own new fixtures passed because it wrote them WITH bold spans — **a campaign that authors its own fixtures can pass its own gate while breaking everyone else's.**

- 🔴 **BASELINE — main carries ~15 standing test failures and the number is GROWING. This is now a hiding place.** Measured 2026-08-13 by `alpha-cia-childless-idle` on a clean pinned run: 2490 tests, 2457 pass, **15 fail**. Composition: 4-5 persona-consumer-probe `ENOENT mkdir '/fixture'` (sandbox root-write restriction, genuinely environmental), 3 AMQ-4 bold-span matcher regressions, plus mutex/herdr-update-rehearsal/waiting-sender-remap/rest-transport-e2e/create-agent-native-quota.
  **THE DANGER IS NOT THE COUNT, IT IS THE HABIT.** Every campaign now triages a long known-failure list and moves on. **The 557-line cap breach survived hours exactly that way** — reported as split by the campaign that never committed the split, then triaged as "known pre-existing" by every campaign after. A standing red is where a real regression goes to be ignored.
  Wanted: triage the list to the file, classify each as environmental / genuine-regression / obsolete, FIX or DELETE the genuine ones, and leave a documented baseline so short that any addition is obvious. Note two entries the Regent has NOT previously catalogued and wants confirmed against unmodified main: **`rest-transport e2e`** and **`waiting-sender remap`**.

- 🔴 **DISTPUB — the WATCH builder defeats the ATOMIC publish. `dist` is a plain directory again, not a symlink.** Found by the Regent 2026-08-13 11:30. `throne-build` runs `nest build --watch`, which writes into `dist/` IN PLACE. `scripts/build-and-publish-dist.mjs` instead builds a `dist.build.<ts>-<pid>` generation and swaps a SYMLINK atomically. **They are two competing publishers of the same path.** At 09:00 `dist` was a symlink to a generation; after the Regent restarted the wedged watcher at 11:19 it is a real directory with a 11:19 mtime, and five orphaned `dist.build.*` generations sit beside it.
  **Why it matters:** the atomic symlink swap exists so a concurrent reader NEVER sees a torn `dist`. Campaign TDW built it; campaign BLD added generation pruning with live-marker protection on top. **An in-place watch build bypasses all of it** — every agent invoking `bin/throne-cli` during a rebuild can read a half-written tree. That is very likely the mechanism behind the `dist-build-atomicity` flakiness seen earlier tonight, and behind at least some "stale dist" confusion: the two publishers disagree about what `dist` even IS.
  **⚠️ REGENT CORRECTION 2026-08-13 12:35 — MY OWN WEDGE DIAGNOSIS WAS UNSOUND AND AT LEAST ONE ALARM WAS FALSE.** I judged "the watcher is stuck" from the mtime of ONE file, `dist/src/tools.js`. **`nest build --watch` is INCREMENTAL: it only rewrites outputs whose inputs changed.** `tools.ts` was untouched by several merges, so its timestamp sat still while everything that DID change rebuilt correctly — the journal showed "Found 0 errors" right on the merge. I restarted the builder for nothing at 12:30. The ~11:15 call rests on the same bad method plus a `THRONE_BUILD_SKEW` report the reporting campaign later traced to its own raw-TypeScript invocation, so **treat it as probably false too.** Only the ~01:35 event stands: real compile errors logged, no subsequent rebuild.
  **THE CORRECT CHECK, which costs the same five seconds: grep `dist/` for a symbol that ACTUALLY CHANGED in the latest commit.** One file's mtime cannot tell you whether an incremental build is current. This nearly entered durable memory as "the watcher goes deaf repeatedly" on the strength of two probably-false positives.
  **Genuinely open:** the watcher has gone deaf ONCE confirmed tonight (~01:35 and ~11:15), each time leaving `dist` frozen for hours while `main` moved, with no journal output and immune to a source `touch`. Both times only a `systemctl --user restart throne-build` recovered it. A build watcher that silently stops is worse than one that crashes, because nothing reports it — the court just quietly runs old code.
  Wanted: decide which publisher owns `dist` and make the other impossible; plus a liveness signal so a wedged watcher is visible rather than inferred hours later from a stale mtime.

- ✅ **BLK — DETECT `blocked` AND PAGE THE REGENT. THE LORD'S ORDER, and he cut my sprawling version down to this.** **(auto-marked at reap: VERIFIED LANDED 7b1402a582b3d379828ae42ed74b371c36b8ae57)** Verbatim: *"I want to detect 'blocked'. whenever something is blocked, regent must be messaged about it and have regent answer it for the agent... agents are instructed not to ask, but again, code is rigid, agents are not."*
  **THE MECHANISM EXISTS AND WE IGNORE IT.** `herdr api schema` (protocol 19) defines a `subscription_event` schema with **`pane_agent_status_changed`**, carrying `agent_status` ∈ {idle, working, blocked, done, unknown} plus pane/workspace/agent/title. There is also **`agent_prompted`**. A grep of `src/` for any subscription returns NOTHING — the throne polls exclusively.
  **LIVE PROOF, 2026-08-13:** `alpha-tio-test-isolation` sat at `blocked` for roughly an hour and the Regent noticed only on a routine sweep. Nothing paged anyone.
  **THE LORD'S DEEPER POINT IS THE OBJECTIVE'S REAL JUSTIFICATION:** AGENTS.md instructs agents never to put questions upward, and they get blocked anyway. *Code is rigid, agents are not.* Same principle that produced EVD and the import ban — **if it matters, tooling must emit it; an instruction will not hold.**
  **SCOPE — deliberately narrow, and the Lord narrowed it himself:** subscribe to the status-change event, and when an agent enters `blocked`, MESSAGE THE REGENT with who/where/why so the Regent can ANSWER IT FOR THE AGENT. Not a general event-driven rewrite.
  **WHAT THE LORD EXPLICITLY RULED OUT, recorded so nobody rebuilds it:** (1) events replacing the no-idling sweep — *"we need the full state regardless to decide if anyone is idle"*, idleness is a family-wide judgement, not a per-pane delta; (2) events fixing the Regent's false-stall call — *"herdr's states are simple so it wouldn't work for your requirement"*, coarse states cannot distinguish a finished verdict gate; (3) the `--force`-on-reap lag — *"that is fine."*
- ✅ **AMQ — `alpha-amq-autumark-queue`** — VERIFIED LANDED `c82b398` (auto-marked at reap; mark position hand-repaired by the Regent, see AMQ-2). (claude/sonnet, throne). **THE LORD'S ORDER:** *"the completion reaper should automark queue. cant trust agent to
  solidly follow instructions, but you can trust code."* Wire ✅-marking into the
  reap path so `trim-queue` works without anyone remembering. **The evidence is this
  Regent: seven campaigns reaped in one night, zero blocks marked.**
