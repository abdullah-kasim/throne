---
name: run-todos
description: Alias for /execute-todos — run/execute a folder of todo files (`todos/<NN>_<description>.md`). Invoke via /run-todos.
version: 0.1.0
user-invocable: true
disable-model-invocation: true
---

# Alias: /run-todos → execute-todos

This is a thin alias. Immediately invoke the `execute-todos` skill and follow its
instructions exactly. Do not duplicate any logic here — `execute-todos/SKILL.md`
is the single source of truth.
