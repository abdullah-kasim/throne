# Worklog

- Run: `{{RUN_LABEL}}`
- Pair: `{{PAIR_ID}}`
- Harness: `{{HARNESS}}`
- Model: `{{MODEL}}`
- Started (UTC): `<fill when work begins>`
- Worklog path: `{{WORKLOG_PATH}}`
- Result path: `{{RESULT_PATH}}`

## Task

{{TASK}}

## Rule-checker coverage

`<One line naming the checker and its coverage/counts, or "Not used">`

Checker output is a lead inventory, not a finding.

## Step N - <what was investigated>

- Inspected and why:
- Tool, command, query, or primary source:
- Observed:
- Concluded:
- Refuted or dead ends:
- Next step, correction, assumption, or unresolved limit:

Repeat the step block as work happens. Do not overwrite earlier entries. Record a
refutation even when it prevents a false positive and therefore produces no
result finding.
